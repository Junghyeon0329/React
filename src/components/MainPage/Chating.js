import './Chating.css';

function Chat() {
	// const [state, setState] = useState({
	// });

	// const updateState = (key, value) => {
	// 	setState((prev) => ({ ...prev, [key]: value }));
	// };

    

	return (
		<div className="grid-item Chating"> </div>
			
	);
};


export default Chat;
